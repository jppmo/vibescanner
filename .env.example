# Redis — used by package validator to cache registry responses (24h TTL)
REDIS_URL=redis://localhost:6379

# Optional: authenticated npm registry lookups (higher rate limits)
NPM_REGISTRY_TOKEN=

# Optional: error tracking
SENTRY_DSN=

# Optional: structured log ingestion
AXIOM_API_KEY=
